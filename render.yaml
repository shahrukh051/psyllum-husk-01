services:
  - type: web
    name: psyllum-husk-01
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py backend.main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
